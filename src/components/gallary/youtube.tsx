import axios from "axios";
import { useEffect } from "react";

const Video = ({ activeMenu }: { activeMenu: number }) => {
  useEffect(() => {
    axios.get("");
  }, []);
  return <div></div>;
};

export default Video;
